spring.jpa.hibernate.ddl-auto=none
spring.datasource.url=jdbc:postgresql://${PGSQL_HOST:localhost}:${PGSQL_PORT:30432}/postgres
spring.datasource.username=${PGSQL_USER:postgres}
spring.datasource.password=${PGSQL_PASSWORD:s3hfUNDagb}

management.endpoint.metrics.enabled=true
management.endpoints.web.exposure.include=*
management.endpoint.prometheus.enabled=true
management.metrics.export.prometheus.enabled=true

instance.name=${INSTANCE_NAME:SPRINGBOOT}

#server.port=8888

debug=true